# Copy to .env.local and fill in your values

# ── Supabase (server-side only — NEVER use NEXT_PUBLIC_ prefix here) ──────────
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Worker Service ─────────────────────────────────────────────────────────────
WORKER_URL=http://localhost:3001

# ── Email Alerts (Resend — https://resend.com) ─────────────────────────────────
# Get API key at: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxx

# From-address: must be from a verified domain in your Resend account.
# For local testing without a domain, use: onboarding@resend.dev
# (can only send to your Resend-registered email address in sandbox mode)
EMAIL_FROM=alerts@yourdomain.com

# ── Monitoring Cron ────────────────────────────────────────────────────────────
# Secret for protecting GET /api/cron/run-monitoring
# Generate with: openssl rand -hex 32
MONITOR_CRON_SECRET=your-random-secret-here

# ── App Base URL (used in email links) ────────────────────────────────────────
# No trailing slash. Used to generate /scan/<id> links in alert emails.
APP_URL=http://localhost:3000

# ── Optional Dev Settings ─────────────────────────────────────────────────────
# Set to true to allow scanning URLs on ports 8080 and 8443
# ALLOW_DEV_PORTS=true
